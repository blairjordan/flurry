mutation UpdatePlayerPrivate($playerId: BigInt!, $aiPrompt: String!) {
  updatePlayersPrivate(
    input: { playerId: $playerId, patch: { aiPrompt: $aiPrompt } }
  ) {
    playersPrivate {
      nodeId
      aiPrompt
    }
  }
}
