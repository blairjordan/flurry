query GetUnacknowledgedAlerts {
  playerAlerts(condition: { acknowledgedAt: null }) {
    nodes {
      id
      nodeId
      alertId
      alert {
        id
        title
        message
        options
        meta
      }
    }
  }
}
